PUSH (en archivo como pushq), guarda valor en una pila
MOV (en archivo como movq, movl), copia datos de un lugar a otro
ADD (en archivo como addl), suma dos valores enteros
SUB (en archivo como subq), restar dos valores enteros
CALL (en tu archivo como call), llama a una función o subrutina